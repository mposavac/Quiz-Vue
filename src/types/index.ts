/*export interface Form {
  time: Array<{ name: string; value: number }>;
  categories: Array<{ name: string; value: string }>;
  difficulty: Array<{ name: string; value: string }>;
  chosenTime: number;
  chosenCategory: string;
  chosenDifficulty: string;
}
*/
